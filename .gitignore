target/
node_modules/
.research/
dist/
*.log

# SSH test keys are generated per machine and per CI run, never committed.
# The public half is generated too — mounting it is cheaper than tracking it,
# and a repository that has never held a private key cannot leak one.
tests/docker/keys/

# Web client dependencies
web/node_modules/

# Swift build output for the mobile client library.
mobile/ios/.build/

# Android build output and the local SDK path, which is per-machine.
mobile/android/build/
mobile/android/app/build/
mobile/android/.gradle/
mobile/android/local.properties

# Generated from project.yml by xcodegen; regenerate rather than commit.
mobile/ios/Omt.xcodeproj/
mobile/ios/build/
mobile/ios/.build/

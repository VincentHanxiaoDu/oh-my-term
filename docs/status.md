# Feature checklist

Every capability the architecture documents, against what the code actually
does. Kept honest deliberately: a row that says ✅ means there is a test, and a
row that says ❌ means somebody would find it missing by using omt.

Generated by hand and reviewed against `docs/reference/capabilities.md`, which
*is* generated — if the two disagree, the generated one is right.

## Core

| | Status | Where |
|---|---|---|
| Terminal emulation, scrollback, reflow | ✅ | `omt-term`, 250+ tests |
| pty spawn, resize, signals, cleanup | ✅ | `omt-pty` |
| Local TUI rendering with damage tracking | ✅ | `omt-tui` |
| Pane geometry and offset rendering | ✅ | `omt-tui/layout.rs`; a pane too small to use is not shown |
| Pane operations from any surface | ✅ | `pane.open` / `close` / `focus` / `list` |
| **Multi-pane TUI** | ✅ | `^A s` splits, `^A o` cycles, `^A x` closes; verified in a 200-column pty |
| **Hint line** | ✅ | `omt-tui/hint.rs` — one hint, state-driven, stops nagging |
| Session tree, workspaces, writer token | ✅ | `omt-session` |
| Capability catalog + parity gate | ✅ | proven to fail when a client lags |
| Layering enforcement | ✅ | `cargo xtask layering`, 28 crates |

## Remote

| | Status | Where |
|---|---|---|
| Unix socket, framing, resume | ✅ | `omt-transport`, `omt-proto` |
| HTTP + WebSocket | ✅ | `omt-server` |
| ssh attach, probe, atomic install | ✅ | 7 tests against a real container |
| **File transfer over ssh** | ✅ | `fs.read`/`fs.write`, chunked; PNG byte-for-byte test |
| Web client: roster, terminal, cards, keys | ✅ | verified in a real browser |
| PWA install + Web Push | ✅ | `push.ts`, `sw.js` |
| **TLS** | ✅ | `omt web --tls-cert C --tls-key K`; refuses to start on an unreadable file |

## Agents

| Agent | Tier reached | Status |
|---|---|---|
| Claude Code | Hook | ✅ incl. `AskUserQuestion` → structured card, no ACP needed |
| Codex | Hook | ✅ notify hook; app-server client ❌ (would raise it to Protocol) |
| Cursor | Hook | ✅ |
| opencode | Protocol | ✅ via generic ACP; dedicated HTTP source ❌ |
| Gemini CLI, Qwen, Goose | Protocol | ✅ via generic ACP |
| Aider, Amp, Crush | Heuristic | ✅ floor only, and it says so |
| Interaction ledger, exactly-once resolve | ✅ | `interaction.respond`, CAS |
| Tier ladder enforced by types | ✅ | a heuristic source cannot emit structured content |

## Workspace

| | Status |
|---|---|
| `fs.list`, `git.status`, `git.diff` | ✅ |
| Config inheritance: global → project | ✅ `omt-config` |
| Import another terminal / VS Code themes and settings | ✅ `omt-theme`, `omt-config` |
| **Built-in theme** | ✅ `Theme::builtin_dark`, passes omt's own contrast rule |
| `theme.get` so a browser renders the user's palette | ✅ |
| `open.recognize` — paths, URLs, file:line | ✅ |
| Command history and suggestions | ✅ `recall.suggest` / `recall.record`, scored per workspace |
| Plugins: install, grant, start, enable/disable | ✅ `plugin.install` / `start` / `list` / `enable`; a plugin is a process holding a token, reaching omt through the same capability surface as any client |
| Scheduled jobs | ✅ `job.create` / `job.list` **and a scheduler that fires them** |
| Voice dictation | ✅ `voice.append` / `voice.clear` / `voice.providers`; pluggable `SttProvider` |
| Persistence across a restart | ✅ `state.save` / `state.restore`, atomic, versioned |
| **Sessions outlive their terminal** | ✅ `omt serve` + `omt attach`; a new process finds the session a previous terminal left |

## Not built

Listed because a checklist that only lists what exists is marketing.

| | Why it is not there |
|---|---|
| A speech engine in the box | The `SttProvider` trait, the registry and `voice.providers` are there, and a third party can add an engine without editing omt. **omt ships none.** That is the design rather than a gap: BYOK means the user's audio goes where the user said, on the user's own account, and omt makes no network call until somebody configures one. |
| Sessions surviving the *daemon* restarting | A session outlives any terminal attached to it, but not `omt serve` itself: a pty's child dies with the process that opened it. Surviving that needs a supervisor that re-parents the child, which is a different piece of work from this one. |
| Codex `app-server` client | Would raise Codex from hook to protocol tier. |
| opencode HTTP (REST + SSE) source | Generic ACP covers opencode today. |
| Native iOS / Android | Scaffold only — `mobile/README.md`. |
| Desktop app | Does not exist. No Electron, no Tauri, nothing. |

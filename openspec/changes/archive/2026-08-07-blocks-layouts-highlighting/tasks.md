# Tasks

- [x] `BlockTracker`: open an early block on output with no open block
- [x] Repair an early block when a prompt mark arrives
- [x] Mark early blocks as unattributed and report it through `session.blocks`
- [x] Launch configuration schema — flat rather than recursive, because omt's
      geometry tiles a flat list and a tree would be flattened immediately
- [x] `layout.open` capability, reporting which pane failed
- [x] Command-line classifier with quote tracking
- [x] Tests for each, including a session that never emits OSC 133
- [x] Docs: research §10 status, architecture 04, `docs/status.md`

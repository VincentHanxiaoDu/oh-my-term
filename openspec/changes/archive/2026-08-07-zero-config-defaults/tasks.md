# Tasks

- [x] `shell.integration` capability: status from observed blocks, plus a
      per-shell snippet
- [x] Snippets for bash, zsh and fish; an honest refusal for anything else
- [x] First-run summary in `run.rs`, printed once, asking nothing
- [x] `config.schema` capability, derived from the setting declarations
- [x] Tests: a shell with marks, a shell without, an unknown shell, a first run
      and a second run
- [x] Wire all three into the web client; parity gate
- [x] Docs: P11 in the principles, the guide, `docs/status.md`

# Tasks

- [x] Instance holds a `UsageLedger` and feeds it every routed agent payload
- [x] `usage.report` capability: per session, total, and headroom
- [x] Codex adapter emits a real `RateLimit` rather than an activity guess
- [x] Tests: accumulation, an unreported session, `Unknown` surviving the wire,
      and no cost invented from tokens
- [x] Wire into the web client; parity gate
- [x] Docs: roadmap entry closed, `docs/status.md`
